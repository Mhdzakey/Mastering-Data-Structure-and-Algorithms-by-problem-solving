#include <iostream>
#include <vector>
#include <deque>
#include <array>
#include <list>
using namespace  std;
int main(){
    /* Practical Details of Array 
    array<int, 5> num = {5, 3, 7, 4, 0};
    int size = num.size();
    cout << "Element at 2 index is " <<num.at(3)<<endl;
    cout << " front element is " << num.front()<<endl;
    cout << " back element is " << num.back()<<endl;
    cout << " front element is " << num.empty()<<endl;
    cout << "  " << num.empty();
    cout << num.size() <<endl;
    for(int i = 0; i < size; i++){
        cout <<num[i]<< " ";
        */
    }
 
   /*Practical Vector
   vector <int> v = { 4, 34, 44, 6, 2};
   cout << v.capacity() << endl;
   cout << v.size() << endl;
   cout << v.at(2) <<endl;
   cout << v.front() << endl;
   cout << v.back() << endl;
   cout << v.empty() << endl;
   */
  /*Deque Practical Demonstration
  int n;
  deque <int> deq = {3, 4, 5, 7, 8};
  cout << deq.front() << endl;
  cout << deq.back() << endl;
  cout << deq.empty() << endl;
  //deq.begin();
  deq.push_front(4);
  deq.push_back(44);
  deq.pop_front();
  deq.pop_back();
  for(int i = 0; i < 7; i++){
    cout << deq[i]<<" "; 
  }
  */
 /*List Practical demonstration
 list<int> li= {44, 56, 23, 4, 6, 2};
 cout << li.empty() <<endl;
 cout << li.back() << endl;
 li.pop_back();
 li.pop_front();
 li.push_back(33);
 li.push_front(333333333);

    
}*/